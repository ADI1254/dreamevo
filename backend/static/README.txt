Place intro_video.mp4 here. It is served at:
  GET /static/intro_video.mp4
when the FastAPI app is running.

The frontend plays this clip once (muted, visual only) when the journey audio starts, then continues audio-only.
